$kind: http-example
request:
  url: "{{baseUrl}}/health"
  method: GET
  headers:
    Accept: application/json
response:
  statusCode: 200
  statusText: OK
  headers:
    Content-Type: application/json
  body:
    type: json
    content: |-
      {
        "key_0": true,
        "key_1": "string"
      }
order: 1000
